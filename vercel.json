{
   "version": 2,
   "builds": [
     { "src": "index.js", "use": "@vercel/node" }
   ],
   "routes": [
     { "src": "/api/workouts/(.*)", "dest": "/api/workouts/$1" },
     { "src": "/api/users/(.*)", "dest": "/api/users/$1" },
     { "src": "/(.*)", "dest": "/index.js" }
   ]
}